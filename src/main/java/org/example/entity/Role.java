package org.example.entity;

public class Role {

    // Skapar tabeller av personer, med roller tilldelade såsom actor eller director
    // id (PK), roleType, creditOrder, movie_id (FK), person_id (FK)
}
